# Default server - catch-all for undefined domains
server {
    listen 80 default_server;
    server_name _;

    # Return 404 for undefined domains
    location / {
        return 404 "Site not found\n";
        add_header Content-Type text/plain;
    }
}
